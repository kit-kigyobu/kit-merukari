<!DOCTYPE html>
<html lang="ja">
<html>
  <head>
    <title>OSAGARY</title>
    <meta charset="UTF-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <%= link_to("OSAGARY", "/", :class=>"navbar-brand" ) %>
<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
  <span class="navbar-toggler-icon"></span>
</button>
      <div class="navbar-collapse collapse slid-down" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto nav">
        <li class="nav-item">
          <%= link_to("OSAGARYとは", "/about") %>
        </li>
        <li class="nav-item">
        <%=link_to("利用規約","/policy")%>
        </li class="nav-item">
        <li>
          <%= link_to("新規登録", "/accounts/sign_up") %>
        </li>

        <% if @current_account_id %>

          <% if @current_user %>

            <li>
              <%=link_to("#{@current_user.name}","/users/show/#{@current_user.id}")%>
            </li>
            <li>
              <%=link_to("出品","/posts/new")%>
            </li>
          <% else %>
            <li>
              まだプロフィール設定が完了していません！<br/>
            </li>
            <%= link_to("プロフィール設定", "/users/signup") %>
          <% end %>

          <li>
            <%= link_to("ログアウト", destroy_account_session_path, {method: :delete}) %>
          </li>

        <% else %>
          <li>
            <%= link_to("ログイン", "/login") %>
          </li>
        <% end %>
    <%=form_tag("/posts/search", :class=>"form-inline") do%>
      <input name="search" class="form-control mr-sm-2" type="search" placeholder="検索..." aria-label="Search">
    <%end%>
        </ul>
      </div>

    </nav>
  </header>

    <% if flash[:notice] %>
     <div class="flash">
       <%= flash[:notice] %>
     </div>
   <% end %>

  <body>

    <%= yield %>
  </body>
</html>
